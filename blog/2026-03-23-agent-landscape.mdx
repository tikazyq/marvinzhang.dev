---
slug: agent-landscape
title: "Mapping the 2026 AI Agent Landscape: From Protocols to Predictions"
authors: ["marvin"]
tags: ["ai", "agents", "architecture", "software-engineering", "open-source"]
date: 2026-03-23
---

import React from 'react';

export const C = { bg: '#06080d', card: '#0d1117', border: '#1e2a3a', text: '#e2e8f0', textSub: '#94a3b8', blue: '#4d8eff', green: '#34d399', amber: '#fbbf24', rose: '#fb7185', purple: '#a78bfa', cyan: '#22d3ee' };

export const ProtocolStack = () => {
  const [active, setActive] = React.useState(null);
  const layers = [
    { id: 'L1', label: 'Human Endpoints', labelZh: '人类端点', color: C.blue, items: [
      { name: 'Mobile', icon: '📱', desc: 'Intent dispatch · Approval · Notification-driven' },
      { name: 'Terminal', icon: '⌨️', desc: 'Text streaming · Pipe composition · Persistent sessions' },
      { name: 'IDE / Desktop', icon: '🖥️', desc: 'ACP access · Generative UI · Diff review' },
      { name: 'Headless', icon: '🤖', desc: 'CI/CD · Webhooks · Bots · IoT' },
    ]},
    { id: 'L2', label: 'Agent↔Human Protocol', labelZh: 'Agent↔人类协议', color: C.purple, items: [
      { name: 'ACP', icon: '🔌', desc: 'JetBrains + Zed — LSP for AI Agents' },
      { name: 'A2UI', icon: '📐', desc: 'Google — Declarative UI spec' },
      { name: 'AG-UI', icon: '🔄', desc: 'CopilotKit — Event-driven bidirectional' },
      { name: 'GenUI', icon: '🎨', desc: 'Pattern — Static / Declarative / Open-ended' },
    ]},
    { id: 'L3', label: 'Agent Harness', labelZh: 'Agent Harness', color: C.amber, items: [
      { name: 'Governance', icon: '🛡️', desc: 'evaluate(action, ctx, state) → Allow | Deny | Modify' },
      { name: 'Runtime', icon: '⚙️', desc: 'Agent loop · Session · Recovery · Token budgeting' },
      { name: 'Capability', icon: '🧰', desc: 'Tool / Skill / Agent hierarchy · Typed schemas' },
      { name: 'Coordination', icon: '🔗', desc: 'Factory · Fractal · Generative adversarial patterns' },
    ]},
    { id: 'L4', label: 'Agent↔Agent/Tool', labelZh: 'Agent↔Agent/Tool', color: C.green, items: [
      { name: 'MCP', icon: '🔧', desc: 'Anthropic — Agent↔Tool, "USB-C for AI"' },
      { name: 'A2A', icon: '🌐', desc: 'Google / LF — Agent↔Agent delegation & mesh' },
      { name: 'Agent Spec', icon: '📋', desc: 'Oracle — Framework-agnostic agent definition' },
      { name: 'AP2', icon: '💳', desc: 'Google — Agent payment protocol' },
    ]},
    { id: 'L5', label: 'Execution Substrate', labelZh: '执行底座', color: C.cyan, items: [
      { name: 'Cloud', icon: '☁️', desc: 'Per-agent isolated containers · Scale-to-zero' },
      { name: 'Local', icon: '💻', desc: 'CLI local · File/Git direct access' },
      { name: 'Hybrid', icon: '🔀', desc: 'On-device + cloud offload' },
      { name: 'LLM', icon: '🧠', desc: 'Model routing · Haiku → Sonnet → Opus tiering' },
    ]},
  ];
  return (
    <div style={{ background: C.bg, borderRadius: 12, padding: 16, margin: '24px 0', border: `1px solid ${C.border}` }}>
      <div style={{ textAlign: 'center', marginBottom: 16, color: C.textSub, fontSize: 13 }}>Click a layer to explore</div>
      {layers.map((layer, i) => {
        const isActive = active === layer.id;
        return (
          <div key={layer.id} style={{ marginBottom: i < layers.length - 1 ? 8 : 0 }}>
            <div
              onClick={() => setActive(isActive ? null : layer.id)}
              style={{
                background: isActive ? layer.color + '22' : C.card,
                border: `1px solid ${isActive ? layer.color : C.border}`,
                borderRadius: 8, padding: '12px 16px', cursor: 'pointer',
                display: 'flex', justifyContent: 'space-between', alignItems: 'center',
                transition: 'all 0.2s',
              }}
            >
              <span style={{ color: layer.color, fontWeight: 600, fontSize: 14 }}>
                {layer.id} · {layer.label}
              </span>
              <span style={{ color: C.textSub, fontSize: 12 }}>{isActive ? '▲' : '▼'}</span>
            </div>
            {isActive && (
              <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))', gap: 8, marginTop: 8, padding: '0 4px' }}>
                {layer.items.map(item => (
                  <div key={item.name} style={{ background: C.card, border: `1px solid ${C.border}`, borderRadius: 8, padding: '10px 12px' }}>
                    <div style={{ fontSize: 14, fontWeight: 600, color: C.text, marginBottom: 4 }}>
                      {item.icon} {item.name}
                    </div>
                    <div style={{ fontSize: 12, color: C.textSub, lineHeight: 1.4 }}>{item.desc}</div>
                  </div>
                ))}
              </div>
            )}
            {i < layers.length - 1 && (
              <div style={{ textAlign: 'center', color: C.textSub, fontSize: 11, padding: '2px 0' }}>↕</div>
            )}
          </div>
        );
      })}
    </div>
  );
};

export const AutomationSpectrum = () => {
  const [selected, setSelected] = React.useState(2);
  const levels = [
    { level: 0, name: 'Manual', short: 'Manual', human: 100, agent: 0, color: C.textSub, shift: 'Memory-driven', time: '4 hours', bugfix: 'Read Jira → search StackOverflow → trace code manually → write fix → wait for review.' },
    { level: 1, name: 'Assisted', short: 'Assist', human: 80, agent: 20, color: C.purple, shift: 'Searching → generating', time: '2 hours', bugfix: 'Paste error to ChatGPT → copy-paste suggested fix → test manually.' },
    { level: 2, name: 'Collaborative', short: 'Collab', human: 55, agent: 45, color: C.blue, shift: 'Writing → reviewing', time: '45 min', bugfix: 'Describe bug in Cursor chat → AI scans codebase → proposes inline fix → human approves with one click.', tag: 'MAINSTREAM' },
    { level: 3, name: 'Semi-Autonomous', short: 'Semi', human: 25, agent: 75, color: C.amber, shift: 'Writing → describing intent', time: '10 min + 15 min agent', bugfix: 'Type one sentence in terminal → agent plans/executes/tests/commits → human approves PR on phone.', tag: 'FRONTIER' },
    { level: 4, name: 'Fully Automatic', short: 'Auto', human: 5, agent: 95, color: C.green, shift: 'Approving → setting boundaries', time: '0 min + 8 min agent', bugfix: 'CI detects failure → headless agent diagnoses and fixes → governance auto-approves → auto-deploys to staging.' },
    { level: 5, name: 'Orchestration', short: 'Orch', human: 2, agent: 98, color: C.cyan, shift: 'Defining workflows → defining goals', time: '30 min + 6 hrs mesh', bugfix: 'Human defines migration goal → architect agent designs plan → coding agents implement in parallel → security agent audits.' },
  ];
  const sel = levels[selected];
  return (
    <div style={{ background: C.bg, borderRadius: 12, padding: 16, margin: '24px 0', border: `1px solid ${C.border}` }}>
      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(6, 1fr)', gap: 4, marginBottom: 16 }}>
        {levels.map((l, i) => (
          <div
            key={i}
            onClick={() => setSelected(i)}
            style={{
              textAlign: 'center', padding: '8px 2px', borderRadius: 6, cursor: 'pointer',
              background: selected === i ? l.color + '33' : C.card,
              border: `1px solid ${selected === i ? l.color : C.border}`,
              transition: 'all 0.2s', minWidth: 0, overflow: 'hidden',
            }}
          >
            <div style={{ fontSize: 13, fontWeight: 700, color: l.color }}>L{l.level}</div>
            <div style={{ fontSize: 9, color: C.textSub, marginTop: 2, overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>{l.short}</div>
            {l.tag && <div style={{ fontSize: 8, color: l.color, marginTop: 2, fontWeight: 600 }}>{l.tag}</div>}
          </div>
        ))}
      </div>
      <div style={{ background: C.card, borderRadius: 8, padding: 16, border: `1px solid ${sel.color}44` }}>
        <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 12 }}>
          <span style={{ color: sel.color, fontSize: 18, fontWeight: 700 }}>L{sel.level} · {sel.name}</span>
          <span style={{ color: C.textSub, fontSize: 12 }}>{sel.time}</span>
        </div>
        <div style={{ marginBottom: 12 }}>
          <div style={{ display: 'flex', height: 24, borderRadius: 6, overflow: 'hidden', border: `1px solid ${C.border}` }}>
            <div style={{ width: `${sel.human}%`, background: C.blue + '88', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: 11, color: '#fff', fontWeight: 600, transition: 'width 0.4s' }}>
              {sel.human > 10 ? `Human ${sel.human}%` : ''}
            </div>
            <div style={{ width: `${sel.agent}%`, background: sel.color + '88', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: 11, color: '#fff', fontWeight: 600, transition: 'width 0.4s' }}>
              {sel.agent > 10 ? `Agent ${sel.agent}%` : ''}
            </div>
          </div>
        </div>
        <div style={{ fontSize: 12, color: C.textSub, marginBottom: 8 }}>
          <strong style={{ color: C.text }}>Key shift:</strong> {sel.shift}
        </div>
        <div style={{ fontSize: 12, color: C.textSub, lineHeight: 1.5, background: C.bg, borderRadius: 6, padding: 12, border: `1px solid ${C.border}` }}>
          <strong style={{ color: C.text }}>Bug fix example:</strong> {sel.bugfix}
        </div>
      </div>
    </div>
  );
};

export const ToolEcosystem = () => {
  const [filter, setFilter] = React.useState('all');
  const protocols = ['MCP', 'ACP', 'A2A', 'A2UI', 'AG-UI'];
  const protocolColors = { MCP: C.green, ACP: C.blue, A2A: C.cyan, A2UI: C.amber, 'AG-UI': C.purple, AP2: C.rose };
  const tools = [
    { name: 'Claude Code', dev: 'Anthropic', type: 'CLI', levels: [2,3,4,5], protocols: ['MCP','ACP'], highlight: true },
    { name: 'Gemini CLI', dev: 'Google', type: 'CLI', levels: [2,3,4], protocols: ['MCP','ACP'] },
    { name: 'Codex CLI', dev: 'OpenAI', type: 'CLI', levels: [2,3,4], protocols: ['MCP','ACP'] },
    { name: 'Aider', dev: 'Open Source', type: 'CLI', levels: [2,3], protocols: ['MCP'] },
    { name: 'OpenCode', dev: 'Open Source', type: 'CLI', levels: [2,3], protocols: ['MCP','ACP'] },
    { name: 'Cursor', dev: 'Cursor Inc', type: 'IDE', levels: [2,3], protocols: ['MCP'] },
    { name: 'Windsurf', dev: 'Codeium', type: 'IDE', levels: [2,3], protocols: ['MCP'] },
    { name: 'JetBrains AI', dev: 'JetBrains', type: 'IDE', levels: [2,3], protocols: ['MCP','ACP'] },
    { name: 'Zed', dev: 'Zed Industries', type: 'IDE', levels: [2,3], protocols: ['MCP','ACP'] },
    { name: 'GitHub Copilot', dev: 'GitHub/MS', type: 'IDE', levels: [1,2,3], protocols: ['MCP','ACP'] },
    { name: 'Claude Code RC', dev: 'Anthropic', type: 'Mobile', levels: [3,4], protocols: ['AG-UI','A2UI'] },
    { name: 'Pocket Agent', dev: 'Community', type: 'Mobile', levels: [3,4], protocols: ['AG-UI'] },
    { name: 'CopilotKit', dev: 'CopilotKit', type: 'Framework', levels: [2,3,4,5], protocols: ['AG-UI','A2UI','MCP'] },
    { name: 'Google ADK', dev: 'Google', type: 'Framework', levels: [3,4,5], protocols: ['A2A','A2UI','MCP'] },
    { name: 'LangGraph', dev: 'LangChain', type: 'Framework', levels: [3,4,5], protocols: ['AG-UI','MCP'] },
    { name: 'BeeAI', dev: 'IBM/LF', type: 'Platform', levels: [4,5], protocols: ['A2A','MCP'] },
  ];
  const typeIcons = { CLI: '⌨️', IDE: '🖥️', Mobile: '📱', Framework: '🔧', Platform: '🌐' };
  const filtered = filter === 'all' ? tools : tools.filter(t => t.protocols.includes(filter));
  return (
    <div style={{ background: C.bg, borderRadius: 12, padding: 16, margin: '24px 0', border: `1px solid ${C.border}` }}>
      <div style={{ display: 'flex', flexWrap: 'wrap', gap: 6, marginBottom: 16 }}>
        <span
          onClick={() => setFilter('all')}
          style={{ padding: '4px 10px', borderRadius: 12, fontSize: 12, cursor: 'pointer', background: filter === 'all' ? C.text + '22' : C.card, color: filter === 'all' ? C.text : C.textSub, border: `1px solid ${filter === 'all' ? C.text + '44' : C.border}` }}
        >All</span>
        {protocols.map(p => (
          <span
            key={p}
            onClick={() => setFilter(filter === p ? 'all' : p)}
            style={{ padding: '4px 10px', borderRadius: 12, fontSize: 12, cursor: 'pointer', background: filter === p ? protocolColors[p] + '22' : C.card, color: filter === p ? protocolColors[p] : C.textSub, border: `1px solid ${filter === p ? protocolColors[p] : C.border}` }}
          >{p}</span>
        ))}
      </div>
      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(220px, 1fr))', gap: 8 }}>
        {filtered.map(t => (
          <div key={t.name} style={{ background: C.card, borderRadius: 8, padding: 12, border: `1px solid ${t.highlight ? C.blue + '66' : C.border}` }}>
            <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 6 }}>
              <span style={{ fontSize: 13, fontWeight: 600, color: t.highlight ? C.blue : C.text }}>{typeIcons[t.type]} {t.name}</span>
              <span style={{ fontSize: 10, color: C.textSub }}>{t.dev}</span>
            </div>
            <div style={{ display: 'flex', gap: 3, marginBottom: 6, flexWrap: 'wrap' }}>
              {t.protocols.map(p => (
                <span key={p} style={{ fontSize: 10, padding: '1px 6px', borderRadius: 8, background: protocolColors[p] + '22', color: protocolColors[p], border: `1px solid ${protocolColors[p]}44` }}>{p}</span>
              ))}
            </div>
            <div style={{ display: 'flex', gap: 2 }}>
              {[0,1,2,3,4,5].map(l => (
                <div key={l} style={{ width: 18, height: 6, borderRadius: 3, background: t.levels.includes(l) ? C.green + 'aa' : C.border + '44' }} />
              ))}
              <span style={{ fontSize: 9, color: C.textSub, marginLeft: 4 }}>L{Math.min(...t.levels)}-L{Math.max(...t.levels)}</span>
            </div>
          </div>
        ))}
      </div>
      <div style={{ fontSize: 11, color: C.textSub, marginTop: 12, textAlign: 'center' }}>
        Showing {filtered.length} of {tools.length} tools {filter !== 'all' && `· filtered by ${filter}`}
      </div>
    </div>
  );
};

Six protocols. Six automation levels. Seventeen tools. Twelve predictions. One interactive map that ties them all together.

The [AI Agent Interaction Landscape](https://agent-landscape.vercel.app) is an open-source, bilingual SPA I built to make sense of how AI agents interact with developers, editors, tools, and each other in 2026. This article walks through the key frameworks it introduces—and the insights that emerged from building it.

{/* truncate */}

## Why a Landscape Map?

The AI agent space in early 2026 feels like the JavaScript framework wars of 2016—except the stakes are higher and the acronyms multiply faster. MCP, ACP, A2A, A2UI, AG-UI, AP2: six protocols from four different organisations, each addressing a different layer of the agent interaction stack. Meanwhile, tools proliferate across CLIs, IDEs, mobile apps, and headless platforms, each supporting different subsets of these protocols at different automation levels.

The landscape map exists because no single article, talk, or documentation site captures the full picture. It's not a product comparison. It's a **protocol stack visualisation** that shows how these pieces fit together—from the human endpoint (your phone, terminal, IDE) down to the execution substrate (cloud containers, local runtimes, LLM routing).

:::info Related Reading
For foundational concepts on AI agent architecture patterns, see [AI Agents: Engineering Over Intelligence](/blog/ai-agents-engineering).
:::

## The Protocol Stack: Five Layers

The core of the landscape is a five-layer protocol stack. Each layer addresses a different boundary in the agent interaction model.

Click any layer below to explore its components:

<ProtocolStack />

## The L0→L5 Automation Spectrum

Perhaps the most useful framework in the landscape is the six-level automation spectrum, modelled after autonomous driving levels. Select any level below to see the human/agent split and a concrete bug fix example:

<AutomationSpectrum />

The industry mainstream sits at **L2** (collaborative), with the frontier pushing into **L3** (semi-autonomous). The jump from L2 to L3 is where the real paradigm shift happens: the developer stops writing code and starts describing intent. The jump from L3 to L4 is even more radical—the developer stops being in the loop at all, and sets governance boundaries instead.

The pattern is clear: human time drops exponentially, but the **weight of each remaining human decision increases**. A 30-second approval at L4 can greenlight 8 hours of agent work.

## A Day in the Life at L2-L4

The landscape includes a "Day in Life" view showing how these automation levels weave together across a real workday:

- **07:30** (Phone, L4): Approve overnight refactoring results with a swipe. 28 files changed, 412 tests passing. 15 seconds.
- **08:15** (Phone, L3): Dispatch a bug fix from Slack via Claude Code Remote Control while ordering coffee.
- **09:30** (IDE, L2): Pair-program OAuth integration with Cursor. Developer architects, AI implements. 2 hours, ~30% human-written.
- **11:45** (Phone, L3): Bug fix from morning complete. Approve from phone, PR auto-merges.
- **14:00** (IDE, L2): AI-assisted code review catches missing index, N+1 query risk.
- **16:00** (Terminal, L4): Configure overnight agents—dependency upgrade + security audit. 5 minutes of setup, 8+ hours of autonomous work.
- **22:00** (Phone, L4): Quick dashboard check. Agent A at 45% progress. Agent B completed, filed 2 tickets. No alerts.

Total: **~3.5 hours** of human time, **~11 hours** of agent time, across 3 devices and 3 automation levels. The phone isn't a coding tool—it's an **approval surface**.

## The Tool Ecosystem: 17 Products Mapped

The landscape maps 17 tools across their protocol support and automation range. Filter by protocol to see how the ecosystem clusters:

<ToolEcosystem />

The key observation: **protocol support determines automation ceiling**. Tools with only MCP cap out around L3. Adding ACP enables IDE integration. Adding A2A + AG-UI unlocks L4-L5 multi-agent mesh.

## Eight Insights

The landscape's "WHY" section distils eight core observations:

1. **"TUI revival isn't nostalgia"** — AI output is natively a text stream. The terminal is the most efficient text stream renderer ever built.

2. **"MUI won't happen"** — There won't be a mobile UI standard for agents. A2UI already makes mobile a native render target.

3. **"Your phone is an approval surface, not a coding tool"** — The bottleneck on mobile isn't input (AI handles that)—it's output. Phones are optimal for approve/reject decisions.

4. **"Four endpoints are projections, not alternatives"** — Mobile, Terminal, IDE, and Headless aren't competing. They're different views of the same agent system.

5. **"Less time, more leverage"** — From L0 to L5, human time drops from 100% to 2%, but impact per decision increases exponentially.

6. **"Governance is the real product"** — At L4/L5, governance is the only thing between agents and production. The most valuable AI infrastructure in 2027 won't be the smartest model—it'll be the best guardrails.

7. **"Protocols > Products"** — MCP, ACP, and A2A will outlive today's AI tools, just like HTTP outlived Netscape.

8. **"The future of coding is async"** — At L4+: set intent → agent works overnight → review in the morning.

## Twelve Predictions with Confidence Scores

The landscape tracks 12 predictions across five dimensions, each with a confidence score and timeline:

### Technology

| Prediction | Confidence | Timeline |
|-----------|------------|----------|
| ACP becomes the LSP of AI coding | 90% | 2026 H2 |
| A2UI kills the "build a mobile app" step | 70% | 2027 |
| Terminals become agent-to-agent interfaces | 45% | 2028+ |

The ACP prediction is the highest-confidence bet in the landscape. JetBrains + Zed co-developed it, GitHub Copilot CLI added ACP in January 2026, and the Agent Registry launched with one-click install. By end of 2026, every major IDE and terminal agent will speak ACP—the "which editor supports which agent" question disappears.

### Career

| Prediction | Confidence | Timeline |
|-----------|------------|----------|
| "Prompt engineering" dissolves into every role | 85% | 2026 H2 |
| 10x developer becomes 100x architect | 70% | 2027 |
| "Software engineer" splits into two careers | 50% | 2028+ |

The career split prediction is provocative: one path toward **System Architects** who design agent orchestration, another toward **Agent Craft specialists** who build individual capabilities. The generalist "full-stack developer" erodes.

### Organisation

| Prediction | Confidence | Timeline |
|-----------|------------|----------|
| Team structure follows automation level | 80% | 2026 H2 |
| "Agent budget" becomes a line item like cloud spend | 60% | 2027 |

### Product

| Prediction | Confidence | Timeline |
|-----------|------------|----------|
| AI-native IDEs lose their moat | 75% | 2026 H2 |
| Agent marketplaces emerge | 55% | 2027 |

### Society

| Prediction | Confidence | Timeline |
|-----------|------------|----------|
| Coding becomes universal but not a profession | 65% | 2027 |
| Always-on agents reshape work-life boundaries | 40% | 2028+ |

The tracker shows 1 prediction fully verified ("prompt engineering dissolves into every role"—supported by a reported 60% decline in LinkedIn prompt engineer job postings), 6 partially verified, 4 pending, and 0 revised—as of March 2026. Several predictions have seen confidence adjustments since initial publication: "agent budget as line item" rose from 60% to 65%, while "A2UI kills the mobile app step" dropped from 70% to 65%.

## Security: Six Threats, Six Defences

As agents push into L4-L5, security becomes the critical bottleneck. The landscape identifies six threat vectors:

1. **Over-permissioned agents** — An L4 agent with unrestricted file system access deletes production config during a refactoring task. **Defence**: Least-privilege per task, allowlists over denylists, time-limited permission envelopes.

2. **Broken trust chains** — Agent A delegates to Agent B via A2A, inadvertently granting broader permissions. **Defence**: Capability downscoping at each delegation hop, permission decay with each handoff.

3. **Prompt injection propagation** — Malicious instructions in code comments hijack agent behaviour, spreading through multi-agent systems. **Defence**: Sanitise at every boundary, canary tokens, cross-validate multi-agent outputs.

4. **Unintended data exfiltration** — An agent sends secrets to external logging during debugging. **Defence**: Data classification labels on MCP resources, network segmentation, token-level redaction.

5. **Governance bypass via tool composition** — Individual tools are safe, but composing them creates dangerous capabilities. **Defence**: Analyse action sequences, estimate blast radius, require human approval above thresholds.

6. **Approval fatigue** — After approving 50 routine requests, a developer rubber-stamps a security vulnerability. **Defence**: Risk-based routing, attention signals for high-impact changes, randomised attention checks.

The most insidious is #6. Every other threat has a technical defence. Approval fatigue is a **human factor** problem that erodes the entire human-in-the-loop safety model.

## The Time-Leverage Paradox

The deepest insight from building this landscape is what I call the **time-leverage paradox**: as human time approaches zero, the value of each remaining human moment approaches infinity.

At L0, you spend 4 hours on a bug fix. Every minute is roughly equal. At L4, you spend 0 minutes—the agent handles it. But when something does require your attention, that 30-second approval decision greenlights 8 hours of autonomous work. At L5, a 30-minute goal-setting session produces days of agent mesh output.

This isn't just a productivity story. It's a fundamental shift in what it means to be a developer. The scarce resource isn't coding time anymore—it's **judgment**. The ability to set the right boundaries, approve the right plans, and catch the right edge cases. Governance is the real product because it's the codification of that judgment.

## Try It

The [AI Agent Interaction Landscape](https://agent-landscape.vercel.app) is open source under MIT at [github.com/tikazyq/agent-landscape](https://github.com/tikazyq/agent-landscape). It's built with React 18 + Vite, weighs ~88KB gzipped, and supports both English and Chinese. It includes a self-assessment quiz that tells you your current automation level and recommends next steps.

Whether you're an L1 developer just starting with AI tools or an L3 developer pushing toward autonomous agents, the landscape is designed to show you where you are, what's possible, and what protocols and tools can get you there.

The agent ecosystem is moving fast. Protocols are being born, tools are converging, and the way developers work is being fundamentally reshaped. The best time to understand the landscape was six months ago. The second best time is now.
